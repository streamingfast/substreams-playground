specVersion: 0.1.0
description: Test WASM substream
codeType: wasm/rust-v1

genesisBlock: 6809737

protoFiles:
- ./pancakeswap.proto

streams:
- name: pairExtractor
  kind: Mapper
  code: ./wasm-filters/pair_extractor/pkg/pair_extractor_bg.wasm
  inputs:
  - proto:sf.ethereum.types.v1.Block
  output:
    type: proto:pcs.types.v1.Pairs

# - name: pairs
#   kind: StateBuilder
#   code: ./wasm-filters/pkg/pairs_state.wasm
#   code: ./pairsState.wasm
#   inputs:
#   - stream:pairExtractor
#   output:
#     storeMergeStrategy: LAST_KEY   # SUM_INTS, SUM_FLOATS
